version: '3.9'

services:
  minecraft:
    build: .
    container_name: minecraft
    ports:
      - "8888:25565"
    volumes:
      - ./data:/data
    environment:
      EULA: "TRUE"
    stdin_open: true
    tty: true
